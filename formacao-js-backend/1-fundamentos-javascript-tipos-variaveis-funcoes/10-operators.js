// == implicit
var num = 1
var text = "1"

console.log(num == text, num === text)
// typeof
console.log(typeof num, typeof text)

// + Sum
// - subtraction
// * multiply
// / divide


// == same value
// === same value and type
// != Different value
// !== Different value and type
// condition
// || or
// && and